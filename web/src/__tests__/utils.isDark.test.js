// Duplicate test moved to `__tests__/utils/isDark.test.js`
// Keep an explicit skipped suite so Vitest doesn't report this file as a missing test suite.
import { describe } from "vitest";

describe.skip("duplicate (moved) - utils.isDark", () => {});
